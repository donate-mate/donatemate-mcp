{
  "name": "@donatemate/figma-plugin",
  "version": "0.1.0",
  "private": true,
  "description": "DonateMate Design Bridge - Figma plugin for Claude integration",
  "scripts": {
    "build": "npm run build:code && npm run build:ui",
    "build:code": "esbuild src/code.ts --bundle --outfile=dist/code.js --target=es2017",
    "build:ui": "cp src/ui.html dist/ui.html",
    "watch": "npm run build && concurrently \"npm run watch:code\" \"npm run watch:ui\"",
    "watch:code": "esbuild src/code.ts --bundle --outfile=dist/code.js --target=es2017 --watch",
    "watch:ui": "chokidar 'src/ui.html' -c 'cp src/ui.html dist/ui.html'"
  },
  "devDependencies": {
    "@figma/plugin-typings": "^1.98.0",
    "chokidar-cli": "^3.0.0",
    "concurrently": "^8.2.2",
    "esbuild": "^0.20.0",
    "typescript": "^5.3.3"
  }
}
