{
  "name": "@donatemate/mcp-oauth-token-customizer",
  "version": "0.1.0",
  "description": "Pre-token generation Lambda trigger for MCP OAuth",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.145",
    "typescript": "^5.6.3"
  }
}
